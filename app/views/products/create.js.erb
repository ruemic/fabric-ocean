<% if @product.new_record? %>
  alert("Failed to upload painting: <% j @product.errors.full_messages.join(', ').html_safe %>");
<% else %>
  $("#products").append("<%= j render(@product) %>");
<% end %>